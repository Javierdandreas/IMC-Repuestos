export interface Brand {
  id: number;
  name: string;
  slug: string;
}

export interface Model {
  id: number;
  name: string;
  slug: string;
}

export interface CedulaData {
  patente: string;
  marca: string;
  modelo: string;
  chasis: string;
}

